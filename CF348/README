div1A / div2C
n×mの行列がある。
"1 r", "2 c", "3 r c x"のクエリが合計q実行される。
"1 r"はr行目をrotate, "2 c"はc列目をrotate, "3 r c x"はその時点のr行目c列目の要素がxであることを表す。
元の行列を出力せよ。
1 <= n,m <= 100, 1 <= q <= 10000

div1B / div2D
1～nの列がある。以下のクエリがq個与えられるので、その実行結果を出力せよ。
クエリ1: i番目の要素を(i+x) mod n番目に移動する
クエリ2: 2*i番目の要素と2*i+1番目の要素を入れ替える
2 <= n <= 10^6, 1 <= q <= 2*10^6, nは偶数

div1D / div2E
