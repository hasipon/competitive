F
n×m のグリッドが与えられる。
そこから辺連結した領域を取って、以下の条件を満たすことが可能かどうかを判定し、可能ならその領域を出力せよ。
- t * 領域面積 == k
- その領域がすべてa[i][j] >= t
- a[i][j] == tとなるi,jが存在する
1 <= n, m <= 1000, 1 <= k <= 10^18, 1 <= a[i][j] <= 10^9

G
h[1], ..., h[n] が与えられる。辺連結な領域を削る方法は何通りあるかをmod 1e9+7で出力せよ。
1 <= n <= 10^6, 1 <= h[i] <= 10^9
